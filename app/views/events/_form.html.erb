<%= form_for(@event) do |f| %>
  <% if @event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% @event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :location%><br>
    <%= f.text_area :location%>
  </div> 
  <div class="field">
    <%= f.label :duration%><br>
    <%= f.time_select :duration%>
  </div>
  <div class="field">
    <%= f.label :start_bound%><br>
    <%= f.datetime_select :start_bound%>
  </div>
  <div class="field">
    <%= f.label :end_bound%><br>
    <%= f.datetime_select :end_bound%>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
